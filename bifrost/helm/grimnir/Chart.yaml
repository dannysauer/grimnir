apiVersion: v2
name: grimnir
description: Wi-Fi CSI human presence detection and room-level localization
type: application

# Chart version — CI-managed placeholder. chart-release.yml rewrites this value
# before packaging, so the checked-in value can legitimately differ from the
# published chart artifact.
version: 0.1.1

# Application version — CI-managed placeholder. release packaging rewrites this
# to the app release tag, and Helm-only chart releases resolve the latest v* tag
# before publishing so appVersion does not silently drift stale.
appVersion: "v0.10.0"

keywords:
  - wifi
  - csi
  - presence-detection
  - iot
  - esp32

home: https://github.com/dannysauer/grimnir
sources:
  - https://github.com/dannysauer/grimnir

maintainers:
  - name: dannysauer
    url: https://github.com/dannysauer
