# Default values for grimnir.
# Override these with --set or a custom values file.

# -- Container images
image:
  geri:
    repository: ghcr.io/dannysauer/grimnir/geri
    # Defaults to .Chart.AppVersion when empty
    tag: ""
    pullPolicy: IfNotPresent
  freki:
    repository: ghcr.io/dannysauer/grimnir/freki
    tag: ""
    pullPolicy: IfNotPresent
  nornir:
    repository: ghcr.io/dannysauer/grimnir/nornir
    tag: ""
    pullPolicy: IfNotPresent
  volva:
    repository: ghcr.io/dannysauer/grimnir/volva
    tag: ""
    pullPolicy: IfNotPresent

imagePullSecrets: []

# -- Number of old ReplicaSets to retain for rollback. Kubernetes default is 10;
# 3 is sufficient for most deployments and avoids accumulation on active clusters.
revisionHistoryLimit: 3

# -- Database connection (required)
database:
  # Connection URL — must use the asyncpg driver prefix:
  #   postgresql+asyncpg://user:password@host:port/dbname
  # For fully automatic first boot on a fresh database, this role must also
  # be able to create the TimescaleDB extension (`CREATE EXTENSION timescaledb`).
  # On PostgreSQL 12 / TimescaleDB 2.11, that typically means a superuser role,
  # unless the extension is preinstalled in the target database.
  url: ""
  # Optional: reference an existing Secret instead of creating one.
  # The Secret must contain the key specified by database.secretKey.
  existingSecret: ""
  secretKey: DATABASE_URL

# -- Optional shared secret for POST /api/models.
# When configured, Freki requires X-Grimnir-Model-Upload-Secret on model uploads
# and Nornir sends the same value automatically. Prefer existingSecret on shared
# clusters so the secret is not committed into values files.
modelUploadAuth:
  sharedSecret: ""
  existingSecret: ""
  secretKey: MODEL_UPLOAD_SHARED_SECRET

# -- Optional shared secret for Nornir's daemon/job control writes.
# When configured, Freki requires X-Grimnir-ML-Control-Secret on daemon
# heartbeats plus job claim/heartbeat/complete/fail, and Nornir sends it
# automatically. This protects machine-only ML control writes; dashboard/API
# auth for human users is still tracked separately in #5.
mlControlAuth:
  sharedSecret: ""
  existingSecret: ""
  secretKey: ML_CONTROL_SHARED_SECRET

# ── Geri (UDP aggregator) ───────────────────────────────────────────────────

geri:
  # Geri is safe to scale horizontally: Kubernetes UDP load-balancing is
  # per-5-tuple (consistent per source IP+port), so each Muninn device will
  # always hit the same replica. DB writes use ON CONFLICT, so all operations
  # are idempotent. A single replica is sufficient for most deployments.
  replicaCount: 1

  resources: {}
  # resources:
  #   requests:
  #     cpu: 100m
  #     memory: 64Mi
  #   limits:
  #     cpu: 500m
  #     memory: 256Mi

  # UDP ingress service — use LoadBalancer so ESP32s can reach it by stable IP.
  # The IP is assigned automatically from your load-balancer pool; set a DNS
  # record pointing AGGREGATOR_HOST (firmware/config.h) at the assigned IP, or
  # use external-dns to do it automatically via the hostname annotation below.
  service:
    type: LoadBalancer
    port: 5005

    # Annotations passed verbatim to the Service.
    # MetalLB pool selection + external-dns hostname (no static IP needed):
    #   metallb.universe.tf/address-pool: default
    #   external-dns.alpha.kubernetes.io/hostname: csi-aggregator.home.example.com
    annotations: {}

  # Extra environment variables for the geri container.
  env:
    UDP_HOST: "0.0.0.0"
    BATCH_SIZE: "50"
    BATCH_TIMEOUT_MS: "500"
    LOG_LEVEL: "info"
    # Prometheus metrics HTTP port. Set to "0" to disable.
    METRICS_PORT: "8001"

# ── Freki (backend API + Hlidskjalf dashboard) ─────────────────────────────

freki:
  # Freki serves shared prediction state from Postgres and is safe to scale
  # horizontally. sessionAffinity is set to ClientIP on the Service to keep
  # long-lived SSE connections on the same pod and avoid unnecessary
  # reconnects during rolling updates.
  replicaCount: 1

  resources: {}

  service:
    type: ClusterIP
    port: 8000
    annotations: {}

  # Ingress for external access to the dashboard and API.
  ingress:
    enabled: false
    className: ""

    # Annotations for external-dns, cert-manager, nginx-ingress, etc.
    # Example — hostname + TLS:
    #   external-dns.alpha.kubernetes.io/hostname: grimnir.home.example.com
    #   cert-manager.io/cluster-issuer: letsencrypt-prod
    #   nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    annotations: {}

    hosts:
      - host: grimnir.example.com
        paths:
          - path: /
            pathType: Prefix

    tls: []

  livenessProbe:
    initialDelaySeconds: 60
    periodSeconds: 15
    failureThreshold: 3

  readinessProbe:
    initialDelaySeconds: 30
    periodSeconds: 10

  # Extra environment variables for the freki container.
  env:
    HOST: "0.0.0.0"
    LOG_LEVEL: "info"

# ── Nornir (ML training daemon) ────────────────────────────────────────────

nornir:
  # Nornir claims training jobs from Freki and trains scikit-learn models.
  # Horizontal scaling is safe: the claim route uses a conditional UPDATE so
  # two daemons cannot claim the same job. Each replica registers itself with
  # a unique DAEMON_NAME (defaults to "nornir-${HOSTNAME}").
  replicaCount: 1

  resources: {}
  # resources:
  #   requests:
  #     cpu: 500m
  #     memory: 512Mi
  #   limits:
  #     cpu: "4"
  #     memory: 4Gi

  # Nornir has no inbound API — it only dials Freki. Metrics are exposed on
  # METRICS_PORT for ServiceMonitor scraping.
  service:
    port: 8001
    annotations: {}

  # Extra environment variables for the nornir container.
  env:
    METRICS_PORT: "8001"
    CLAIM_INTERVAL_S: "10"
    HEARTBEAT_S: "30"
    LOG_LEVEL: "info"

# ── Völva (live inference service) ─────────────────────────────────────────

volva:
  # Völva subscribes to Freki's /api/csi-stream and publishes predictions
  # back via PUT /api/predictions/current. Only one replica should run —
  # multiple writers would race on the single-row predictions cache.
  replicaCount: 1

  resources: {}

  service:
    type: ClusterIP
    port: 8002
    annotations: {}

  livenessProbe:
    initialDelaySeconds: 30
    periodSeconds: 15
    failureThreshold: 3

  readinessProbe:
    initialDelaySeconds: 15
    periodSeconds: 10

  # Extra environment variables for the volva container.
  env:
    HOST: "0.0.0.0"
    WINDOW_SIZE: "50"
    MODEL_REFRESH_S: "30"
    LOG_LEVEL: "info"

# ── Vertical Pod Autoscaler ─────────────────────────────────────────────────
# Requires the VPA CRD to be installed in the cluster.
# https://github.com/kubernetes/autoscaler/tree/master/vertical-pod-autoscaler
vpa:
  enabled: false
  # "Off"     — emit recommendations only; no pod changes (safe starting point)
  # "Initial" — apply recommendations at pod creation; no evictions
  # "Recreate"— apply and recreate pods to right-size (mild disruption)
  # "Auto"    — apply and evict pods continuously (can disrupt SSE connections)
  updateMode: "Off"
  geri:
    minAllowed:
      cpu: 50m
      memory: 64Mi
    maxAllowed:
      cpu: "2"
      memory: 512Mi
  freki:
    minAllowed:
      cpu: 50m
      memory: 128Mi
    maxAllowed:
      cpu: "2"
      memory: 512Mi
  nornir:
    minAllowed:
      cpu: 100m
      memory: 256Mi
    maxAllowed:
      cpu: "4"
      memory: 4Gi
  volva:
    minAllowed:
      cpu: 50m
      memory: 128Mi
    maxAllowed:
      cpu: "2"
      memory: 1Gi

# ── Prometheus metrics ──────────────────────────────────────────────────────
# Geri:   exposes /metrics on a dedicated port (geri.env.METRICS_PORT).
# Freki:  exposes /metrics on the same HTTP port as the API (freki.service.port).
# Nornir: exposes /metrics on a dedicated port (nornir.env.METRICS_PORT).
# Völva:  exposes /metrics on the same HTTP port as /health (volva.service.port).
prometheus:
  serviceMonitor:
    # Create ServiceMonitor resources for Prometheus Operator discovery.
    # Requires prometheus-operator CRDs (e.g. kube-prometheus-stack).
    enabled: false
    # Namespace for the ServiceMonitors; defaults to the release namespace.
    namespace: ""
    interval: 15s
    scrapeTimeout: 10s
    # Additional labels to match your Prometheus Operator's serviceMonitorSelector.
    # kube-prometheus-stack default requires no extra labels (matchAll).
    labels: {}

# ── Grafana dashboard ───────────────────────────────────────────────────────
grafana:
  dashboard:
    # Create a ConfigMap containing the Grimnir Grafana dashboard.
    # Requires the Grafana sidecar (bundled with kube-prometheus-stack).
    # The sidecar watches for ConfigMaps with the label below and auto-imports them.
    enabled: false
    # Folder to place the dashboard in (the dashboard name is always "Grimnir").
    folder: "Grimnir"
    # Label key and value that the Grafana sidecar watches for.
    # kube-prometheus-stack default: grafana_dashboard: "1"
    sidecarLabel: grafana_dashboard
    sidecarLabelValue: "1"
