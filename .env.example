# Database — external PostgreSQL + TimescaleDB on humpy (not containerised)
DATABASE_URL=postgresql+asyncpg://csi_user:changeme@humpy.home.arpa:5432/csi

# Aggregator UDP listener port (ESP32 receivers stream to this)
UDP_PORT=5005

# Backend HTTP port
BACKEND_PORT=8000

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
